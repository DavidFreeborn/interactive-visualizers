import React from 'react';
import { SignalingGameView } from '../ui/components/SignalingGameView';

/**
 * Public-facing classic signaling-games app entry point.
 */
export function ClassicSignalingGamesApp(): React.ReactElement {
  return <SignalingGameView />;
}
